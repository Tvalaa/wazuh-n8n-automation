## Setup
Before running the stack, set your own dashboard API password in:
- `wazuh-docker/single-node/config/wazuh_dashboard/wazuh.yml` (replace `CHANGE_ME_SEE_README`)
- `wazuh-docker/single-node/config/wazuh_indexer/internal_users.yml` (matching value)

This keeps credentials out of version control.
